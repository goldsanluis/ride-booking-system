# TODO: Modernize PUP Rides Login UI (Tkinter)

- [x] Inspect current `gui/login_window.py` (done)
- [ ] Update PUP palette constants to required values
- [ ] Redesign `_setup_ui()` header with improved hero-style section
- [ ] Redesign `_entry()` to emulate rounded input fields via layered frames
- [ ] Restyle password toggle row (`_pw_row`) to match new input/button look
- [ ] Restyle primary button (`_btn_primary`) with `#FFD700` background and `#1a0000` text
- [ ] Increase spacing across login/register screens for a cleaner layout
- [ ] Run app (`python main.py`) and visually verify UI

